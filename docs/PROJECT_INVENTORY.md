# Project Inventory

> Written 2026-09-15 from a direct directory listing of the repository (not assumed from
> filenames). Generated/gitignored paths (`.venv/`, `android/build/`, `__pycache__/`, `.pytest_
> cache/`) are summarized, not enumerated file-by-file.

## Backend (`backend/`)

| File | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `main.py` | Python | FastAPI app entry point | **YES — critical** | No | The whole API tier starts here |
| `api/auth.py` | Python | Register/login routes | YES | No | — |
| `api/transcribe.py` | Python | Whole-file + WS transcription routes | **YES — critical** | No | Core feature entry point |
| `api/sessions.py` | Python | Session list/detail/delete | YES | No | — |
| `api/teacher.py` | Python | Teacher enrollment routes | YES | No | — |
| `api/minutes.py` | Python | Minutes retrieval/export routes | YES | No | — |
| `api/deps.py` | Python | Shared FastAPI dependency re-exports | Minor | No | One-liner convenience file |
| `core/config.py` | Python | All environment variables/settings | **YES — critical** | No | Touch with care — every env var name is a contract |
| `core/security.py` | Python | JWT + bcrypt auth logic | **YES — critical** | No | — |
| `core/logging.py` | Python | Structured JSON logging | YES | No | Must stay starlette-free (worker tier depends on this) |
| `core/rate_limit.py` | Python | slowapi rate limiting + upload-size guard | YES | No | — |
| `core/request_context.py` | Python | Request-ID middleware | Minor | No | — |
| `core/pubsub.py` | Python | Worker↔API result fan-out | **YES — critical** | No | — |
| `models/user.py`, `session.py`, `teacher.py` | Python | SQLAlchemy ORM models | **YES — critical** | No | Any schema change needs a matching Alembic migration |
| `schemas/*.py` | Python | Pydantic request/response shapes | YES | No | — |
| `services/audio_service.py` | Python | Pipeline orchestrator | **YES — critical** | No | The heart of the ML pipeline |
| `services/diarization_service.py` | Python | pyannote wrapper | YES | No | — |
| `services/teacher_verification_service.py` | Python | SpeechBrain wrapper | YES | No | — |
| `services/glossary_service.py` | Python | Code-switch term correction | YES | No | — |
| `services/keyword_service.py` | Python | TextRank | YES | No | — |
| `services/minutes_service.py` | Python | Rule-based minutes generation | YES | No | — |
| `services/session_service.py` | Python | Session persistence + finalize logic | **YES — critical** | No | — |
| `services/user_service.py` | Python | Auth persistence | YES | No | — |
| `worker/celery_app.py` | Python | Worker entry point + model loading | **YES — critical** | No | — |
| `worker/tasks.py` | Python | The actual Celery task bodies | **YES — critical** | No | — |
| `database/db.py` | Python | SQLAlchemy engine/session setup | **YES — critical** | No | — |
| `database/migrations/` | Alembic | Schema migration history | YES (Postgres path only) | No | — |
| `utils/audio_io.py` | Python | Temp-file helpers | YES | No | — |
| `data/glossary.json` | Data | Trilingual term-correction list | YES | No — but freely editable | Hand-maintained; grow it from real transcription errors |

## Flutter client (`android/`)

| File/dir | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `lib/main.dart`, `lib/app.dart` | Dart | App entry point + auth-gated routing | **YES — critical** | No | — |
| `lib/core/*.dart` | Dart | Networking, audio encoding, auth, settings | **YES — critical** | No | Everything else depends on these |
| `lib/screens/**/*.dart` | Dart | The six required screens | **YES — critical** | No | — |
| `lib/models/*.dart` | Dart | Request/response shape mirrors of backend schemas | **YES — critical** | No | Field names are a cross-language contract |
| `lib/widgets/*.dart` | Dart | Reusable small widgets | YES | No | — |
| `test/*.dart` | Dart | Unit + widget tests | YES | No | 69 passing, confirmed this pass |
| `pubspec.yaml` | Config | Dependencies + app metadata | **YES — critical** | No | — |
| `pubspec.lock` | Generated | Locked dependency versions | Generated | Yes, regenerates via `flutter pub get` | Don't hand-edit |
| `analysis_options.yaml` | Config | Lint rules | Minor | No | — |
| `android/local.properties` | Generated (machine-specific) | SDK paths | Machine-specific | Yes, regenerated on next build | **Never commit this file's real content to a shared repo if paths differ per machine** |
| `android/app/build.gradle.kts` | Config | Android build config, signing | **YES — critical** | No | Contains the two known TODOs (App ID, signing) |
| `android/app/src/main/AndroidManifest.xml` | Config | Permissions, app label, cleartext-traffic flag | **YES — critical** | No | — |
| `build/` | Generated | Build output | Generated | **Yes, fully** | Regenerated by `flutter build`/`flutter run` |
| `.flutter-plugins-dependencies`, `flutter_01.log` | Generated | Tooling metadata/logs | Generated | Yes | — |

## AI/fine-tuning (`ai/finetuning/`)

| File | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `finetune_whisper.py` | Python | LoRA/PEFT training script | YES (for future work) | No | Never run against real data yet |
| `convert_to_faster_whisper.py` | Python | Merge + convert to CTranslate2 | YES (for future work) | No | — |
| `README.md` | Docs | Scaffold usage notes | Minor | No | — |

## Evaluation (`evaluation/`)

| File | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `wer.py` | Python | WER/CER computation | YES (for future evaluation) | No | Zero real results exist yet |
| `teacher_id.py` | Python | Precision/recall/F1/FAR/FRR | YES | No | Same |
| `latency.py` | Python | Per-stage latency + RTF | YES | No | Source of the numbers in `docs/ML_PIPELINE.md` |
| `sus.py` | Python | Standard 10-item SUS scoring | YES | No | Scoring math only, no respondents yet |
| `resources.py` | Python | CPU/memory sampling via psutil | YES | No | — |

## Scripts (`scripts/`) — dev utilities, not imported by the app

| File | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `record_test_audio.py` | Python | Mic capture CLI | Minor | Yes, functionally | Kept for convenience |
| `preprocess_audio.py` | Python | Standalone FFmpeg standardization | Minor | Yes, functionally | Logic duplicated into `backend/services/` |
| `transcribe_audio.py` | Python | Standalone Whisper CLI | Minor | Yes, functionally | — |
| `process_pipeline.py` | Python | Standalone full-pipeline CLI | Minor | Yes, functionally | — |
| `simulate_streaming.py` | Python | Chunked-streaming simulation CLI | Minor | Yes, functionally | — |
| `diarize_audio.py` | Python | Standalone pyannote CLI | Minor | Yes, functionally | — |
| `v2_smoke_test.py`, `ws_smoke_test.py` | Python | End-to-end smoke tests against a running server | YES — genuinely useful, **confirmed working end-to-end from any working directory, on the first request, this pass** (real register/login/transcribe/enroll/WS run, real output, `ALL SMOKE TESTS PASSED`) | No | Not part of `pytest`; run manually. `v2_smoke_test.py` previously required `scripts/` as the working directory (a hardcoded relative path); fixed this pass to resolve `recordings/` via `Path(__file__).resolve().parent.parent`, same as `ws_smoke_test.py` already did. See `docs/TROUBLESHOOTING.md`. |
| `tester` | Python (no extension) | 6-line mic-device scratch check | **NO** | **Yes** | See `docs/DEVELOPMENT.md`'s Known Bugs list |

## Tests (`tests/`)

16 files, one per major backend module — see `docs/BACKEND.md`/`docs/DEVELOPMENT.md` for detail.
All important, none safe to delete, all currently passing.

## Root-level files

| File | Type | Purpose | Important? | Safe to delete? | Notes |
|---|---|---|---|---|---|
| `CLAUDE.md` | Docs | Master handoff document | **YES — critical** | No | — |
| `README.md` | Docs | Narrative overview | YES | No | Kept consistent with CLAUDE.md as of the last docs pass |
| `THESIS_...(4).docx` | Document | The actual manuscript | **YES — critical, but not code** | No | Never modify programmatically; treat as the owner's document |
| `LICENSE` | Directory (empty) | — | **A known gap** | N/A | Should be a file, isn't — deliberately left for the owner to decide |
| `requirements.txt` | Config | Full-union pip install target | **YES — critical** | No | Intentionally unpinned |
| `requirements-api.txt` | Config | Lean API-tier subset | YES | No | Used by `Dockerfile.api` |
| `requirements-worker.txt` | Config | Full ML worker-tier subset | YES | No | Used by `Dockerfile.worker` |
| `alembic.ini` | Config | Alembic entry config | YES | No | — |
| `.gitignore` | Config | Git exclusions | YES | No | — |
| `.dockerignore` | Config | Docker build-context exclusions | YES | No | Includes `*.docx` — the manuscript must never enter a Docker build context |
| `tester_terminal.txt` | Text | Scratch note of manual CLI commands used to test the four earliest scripts (`record_test_audio.py`, `preprocess_audio.py`, `transcribe_audio.py`, `process_pipeline.py`) — read in full this pass | Minor | Yes | Also evidence the project once lived under a `OneDrive\Documents\Thesis\...` path before moving to its current location — not referenced by any code |

## Data / model / output directories

| Directory | Contents | Tracked by git? | Safe to delete? |
|---|---|---|---|
| `datasets/` | `raw/ clean/ processed/ metadata/`, all empty except `.gitkeep` files | Yes (the `.gitkeep`s only) | The folders themselves, no — fine-tuning scripts expect them to exist |
| `models/` | Currently holds a real, downloaded SpeechBrain ECAPA-TDNN checkpoint | **No — gitignored** | Yes, re-downloads on next use |
| `recordings/` | Sample audio used in development (`classroom.wav`, `lecture_preprocessed.wav` + chunks) | **No — gitignored** | Yes, individually |
| `transcripts/` | Empty; auto-created at import time by `backend/core/config.py`, unused otherwise | No (empty, untracked) | Yes |
| `integration/`, `experiments/` | Completely empty, no code references found | No (empty, untracked) | Yes |
| `deployment/` | Dockerfiles + compose + `.env.example` | Yes | No |
| `docs/` | This documentation set + pre-existing docs | Yes | No |
