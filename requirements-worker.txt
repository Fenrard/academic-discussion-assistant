# The worker tier's dependency footprint — deployment/Dockerfile.worker installs
# ONLY this file. This is where every ML library actually lives; the API tier
# (requirements-api.txt) doesn't need any of it.

celery
redis
sqlalchemy
psycopg[binary]
networkx

soundfile
numpy
faster-whisper
torch
torchaudio
silero-vad
pyannote.audio
# (denoise — enable_denoise — is now FFmpeg's afftdn filter, in backend/services/audio_service.py:
#  clean_audio(); no Python package. pyrnnoise/RNNoise was dropped after pyrnnoise 0.4.3 broke against
#  every installable audiolab/PyAV combo — see docs/paper-vs-implementation.md §3.3.)
speechbrain       # optional: teacher verification (ECAPA-TDNN), enable_teacher_verification=False skips it
